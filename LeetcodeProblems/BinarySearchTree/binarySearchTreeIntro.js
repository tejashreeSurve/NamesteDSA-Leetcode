/**
 * BST is a special typeof binary tree with atmost 2 children 
 * For every node :
 *  all node of left subtree have smaller value
 *  all node of right subtree have greater value
 * 
 * searching element in  binary tree is very very fast  with time complexcity- O(log n)
 * inroder traversal of BTS is always sorted
 * 
 */